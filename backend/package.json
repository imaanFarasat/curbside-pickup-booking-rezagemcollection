{
  "name": "rezagem-booking-backend",
  "version": "1.0.0",
  "description": "Backend API for Reza Gem Collection curbside pickup booking system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "build": "cp -r ../frontend ./frontend && cd frontend && npm install --production=false && npm run build && cd .. && mkdir -p public && cp -r frontend/build/* public/ && rm -rf frontend",
    "build:test": "ls -la && ls -la ../frontend/public && cp -r ../frontend ./frontend && ls -la frontend/public && cd frontend && npm install --production=false && npm run build && cd .. && mkdir -p public && cp -r frontend/build/* public/ && rm -rf frontend"
  },
  "keywords": ["booking", "curbside", "pickup", "rezagem"],
  "author": "Reza Gem Collection",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.0",
    "sequelize": "^6.32.1",
    "nodemailer": "^6.9.4",
    "express-rate-limit": "^6.10.0",
    "helmet": "^7.0.0",
    "express-validator": "^7.0.1",
    "moment-timezone": "^0.5.43",
    "uuid": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.6.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 